#local settings for platformIO
.pio

# everything realted to .vscode is for the vscode setup 
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.vscode/setting.json

# main.cpp is not included in order to keep it clean even though it's used for testing
src/main.cpp

# does not need to be synched, just for mkdocs locally
site/